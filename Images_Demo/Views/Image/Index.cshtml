@model IEnumerable<Picture>

@foreach (var item in Model)
{
    <div class="container-fluid my-5">
        <h1>@item.Name</h1>
        <img class="img-thumbnail" src="data:image/png;base64,@Convert.ToBase64String(item.Image)" />
    </div>
}
